{
  "name": "rise-beacon",
  "version": "1.0.0",
  "description": "Standardized error handling and classification for Rise projects",
  "peerDependencies": {
    "http-status-codes": ">=2.0.0"
  },
  "dependencies": {
    "http-status-codes": "^2.3.0"
  },
  "devDependencies": {
    "typescript": "^5.4.2"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "generate-docs": "ts-node scripts/generate-error-docs.ts",
    "prepare": "npm run build && npm run generate-docs"
  }
}
